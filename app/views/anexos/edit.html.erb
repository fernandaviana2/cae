<p><center> <b><font style="font-size:30pt; color:LimeGreen; line-height:30pt; type=ArialBlack"> Atualizando Documentos: </font></b></center></p> 
<%= flash_message %>

<%= form_for bolsista_anexo_url, :html => { :multipart => true } do |f| %>  
  <%= render partial: "form", locals: {f: f} %>  
  <%= f.submit "Atualizar" %>
  <b><%= link_to 'Voltar', bolsista_url(@bolsista) %></b>
<% end %>
